#include<iostream>
#include <stdio.h>

using namespace std;

struct emp
{
    string name;
    int sc;
};
int main()
{
    emp a, b;
    a.name = "qq";
    b.name = "ww";
    a.sc = 1;
    b.sc = 2;
    emp ar[3];
    ar[0] = a;
    ar[1] = b;

    cout << ar[1].name << endl;


    // string a = "wwww";
    // int aa = a[0] == 'w' ? 1 : 0;
    // cout << aa<< endl;
    // bool a = 'a' <= 'x' <= 'z';bool a1 = 'a' <= 0 <= 'z';
    // cout << a << " " << a1;
    // char tmp[20];
    // int a = 3;

    // sprintf(tmp, "@%d", a);
    // //sprintf(s, "%d", 123);  //把整数123打印成一个字符串保存在s中
    // // sprintf(s, "%8x", 4567);  //小写16进制，宽度占8个位置，右对齐
    // cout << tmp;
    //cout<< "\t.zero\t4" << endl;


}
/*
system("pause") 相当于从程序里调用“pause”命令，继而暂停。
必须添加”stdlib.h" 或 “cstdlib"头文件。
但只适合DOS或Windows，不合适合Linux，
这就是出现“sh: PAUSE: command not found" 的原因

const char 

*/